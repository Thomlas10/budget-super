{
  "name": "budget-super",
  "version": "1.0.0",
  "scripts": {
    "dev": "npx browser-sync start --server --files '*.html, *.css, *.js'"
  },
  "devDependencies": {
    "playwright": "^1.61.0"
  }
}
